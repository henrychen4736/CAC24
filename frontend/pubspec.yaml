name: tennis_analyze
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # firebase_core: ^3.6.0
  # cloud_firestore: ^5.4.4
  image_picker: ^0.8.9
  intl: ^0.17.0
  http: ^0.13.3
  logging: ^1.3.0
  video_player: ^2.9.2
  path_provider: ^2.1.4
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
