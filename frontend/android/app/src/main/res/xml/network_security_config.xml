<?xml version="1.0" encoding="utf-8"?>
<!-- Release/profile builds: HTTPS only, except plain HTTP to the Android emulator's
     alias for the host machine (10.0.2.2) and loopback. Host the analysis server
     behind HTTPS for real users. Debug builds override this file with
     src/debug/res/xml/network_security_config.xml. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
