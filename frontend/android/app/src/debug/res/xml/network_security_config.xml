<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: allow plain HTTP to any host so a phone can reach an
     analysis server running on your computer's LAN address (e.g. http://192.168.1.20:8000). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
