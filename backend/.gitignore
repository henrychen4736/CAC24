.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Datasets, extracted features, runtime uploads, server log from bootstrap.py --run.
# Anchored with a leading "/" so they only match backend/<name>: an unanchored
# "data/" also matched src/tennis_ai/data/ and kept package files out of git.
/data/
/uploads/
/server.log

# Downloaded / trained model artifacts (see README for how to fetch or train them)
/models/*
!/models/README.md
